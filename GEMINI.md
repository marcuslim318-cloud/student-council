# Gemini CLI 入口（学生会报销记账系统）

本项目的唯一宪法真源是 `AGENTS.md`。请先读取并遵守它：

@AGENTS.md

## Gemini 专属补充

- 项目使用中文界面与中文沟通，除非用户明确要求英文。
- 运行命令使用 `npm.cmd run dev` / `npm.cmd run build`（Windows PowerShell 禁脚本）。
- 本文件仅作入口；规则冲突一律以 `AGENTS.md` 为准。若 `AGENTS.md` 更新，本文件无需同步正文。